boss: guard_captain
runs: 600

compositions:
  - name: all_sweaty
    classes: [gunner, vanguard, vanguard, blade_dancer, blade_dancer]
    specs: [assault, shield, blade, multi_blade, multi_blade]
    profiles: [sweaty, sweaty, sweaty, sweaty, sweaty]
    win_rate:
      min: 0.60
      max: 0.95
  - name: mixed_average
    classes: [gunner, vanguard, vanguard, blade_dancer, blade_dancer]
    specs: [assault, shield, blade, multi_blade, multi_blade]
    profiles: [average, average, average, average, average]
    win_rate:
      min: 0.25
      max: 0.85
  - name: all_bad
    classes: [gunner, vanguard, vanguard, blade_dancer, blade_dancer]
    specs: [assault, shield, blade, multi_blade, multi_blade]
    profiles: [bad, bad, bad, bad, bad]
    win_rate:
      min: 0.0
      max: 0.50

win_rate:
  min: 0.15
  max: 0.90

duration:
  min_seconds: 15
  max_seconds: 600

phase_reach:
  - phase: 2
    min_rate: 0.60
  - phase: 3
    min_rate: 0.30

tree_health:
  max_dead_nodes: 10
  max_cold_nodes: 20

spec_balance:
  max_damage_share_sigma: 0.30

ability_stats:
  - ability: melee_swipe
    max_kill_rate: 0.50
  - ability: ground_slam
    max_kill_rate: 0.60
  - ability: bull_charge
    max_kill_rate: 0.60
